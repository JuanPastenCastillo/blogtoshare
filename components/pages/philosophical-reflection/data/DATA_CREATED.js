import { LayoutFoundersData } from "../../../../utils/LayoutFoundersData"
import { DATA_TO_SHOW } from "./DATA_TO_SHOW"
import { MULTIPLE_REFERENCES } from "./MULTIPLE_REFERENCES"

const TITLE = "Starting from scratch with love, wisdom and philosophy. Spoiler alert: the key is asking good questions"

export const DATA_CREATED = LayoutFoundersData({
  TITLE,
  DATA_TO_SHOW,
  isMultipleSources: true,
  MULTIPLE_REFERENCES
})
